[project]
name = "checkpoint-safe"
authors = ["Checkpoint Team"]
description = "Secure asset tokenization and compliance platform"
telemetry = false
requirements = []
[contracts.asset-checkpoint]
path = "contracts/asset-checkpoint.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
